const dotenv = require('dotenv'); dotenv.config(); fetch('https://generativelanguage.googleapis.com/v1beta/models?key=' + process.env.GEMINI_API_KEY).then(res => res.json()).then(data => { if(data.models) { console.log(data.models.map(m => m.name).filter(n => n.includes('flash') || n.includes('image') || n.includes('imagen') || n.includes('3'))); } else { console.log('No models', data); } }).catch(console.error);
