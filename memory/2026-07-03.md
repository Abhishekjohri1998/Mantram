# Daily Log — 2026-07-03

## Accomplishments
- Restricted Advanced Mode video generator and Storyboard Mode slider for `seedance-2.0-mini` in `AdvancedMode.jsx` and `Storyboard.jsx` to a maximum duration of **45 seconds** (`[5, 10, 15, 20, 30, 45]`), while dynamically calculating the slider's max bounds based on the model's configured durations.
- Increased maximum uploaded character avatar limit (`avatarImages`) from 4 to 9 in `Storyboard.jsx` (frontend) and `video-studio.js` (backend multer fields and limiters).
- Increased maximum location/scene reference image limit (`refImages`) from 3 to 9 in `Storyboard.jsx` (frontend) and `video-studio.js` (backend multer fields, limiters, and URL parsers).
- Resolved location image propagation loophole: updated `combinedReferences` and `longFormRefs` inside the `/storyboard/animate` route to include location reference images (`dbRefImageUrls`), forwarding them to the Atlas Cloud generation payload.
- Integrated multi-avatar details, location references, and logo visual elements dynamically into the `@imageN` tag legend built in `generateAnimateVideoPrompt`, ensuring Claude provides aligned cinematic prompts.
- Updated `storyboardLongForm.js` reference image slicing limit from 6 to 9 to allow all product, character, style, and location images to be passed to Atlas Cloud.
