# Daily Memory - 2026-06-19

## Tasks Completed
- Fixed critical canvas focus-stealing bug in `FlowCanvas.jsx` which was:
  - Stealing focus from the textareas and inputs inside node types (e.g. `ImageGenerateNode`).
  - Bubbling click events on the "+ Add Node" button to pane click, immediately closing the node menu.
  - Aborting node connection drags on mouse down because of focus change.
- Fixed typing lag, cursor jumping, and focus loss in `ImageGenerateNode` and `VideoGenerateNode` prompt textareas:
  - Introduced debounced prompt text state syncing (500ms delay) to prevent instant network graph commands on every keystroke.
  - Passed `onBlur` handlers to immediately sync prompt values when user clicks away.
- Added `nodrag` class to standard inline elements (select, textarea, text/number inputs) in `BaseNode`'s `InlineField` to prevent drag event interference.
- Tested and verified compiling frontend, ran backend unit tests cleanly.
- Verified all canvas actions pass in automated Playwright integration test (`node backend/scratch/verify-canvas.js`).
