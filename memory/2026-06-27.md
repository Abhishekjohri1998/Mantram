# Memory Log - June 27, 2026

- **Action**: Resolved LLM timeout errors during storyboard generation when falling back to native Gemini.
- **Backend changes**:
  - `backend/ai/router.js`: Increased request timeout `PER_CALL_TIMEOUT_MS` from 12 seconds to 45 seconds. This gives reasoning models (like Gemini 2.5 Pro) sufficient time to "think" and output long structured JSON storyboards without triggering premature abort errors.

- **Action**: Optimized MongoDB queries for projects to prevent memory limit errors.
- **Backend changes**:
  - `backend/models/VideoProject.js`: Added a single-key index on `createdAt: -1`. This ensures that system-wide dashboard and superadmin queries sorting by creation date are fully index-supported, preventing MongoDB from throwing the `QueryExceededMemoryLimitNoDiskUseAllowed` error.

- **Deployment changes**:
  - Pulled latest updates from `main` branch, merged them with local fixes, committed the changes, and pushed them to the `staging` branch.

- **Action**: Integrated Google Veo 3.1 Lite model from Atlas Cloud in the Video Studio.
- **Backend changes**:
  - Registered `veo-3.1-lite` under model capability lists (`falClient.js`), pricing structure maps (`pricingMonitor.js` & `superadmin.js`), schema validators (`VideoProject.js`), prompt enhancer cinematic rules (`promptEnhancer.js`), and director LLM settings (`videoAgentDirector.js` & `video-studio.js`).
  - Added segment duration limits (8s optimal, 8s max) for `veo-3.1-lite` to the long-form generator cost estimator (`longFormGenerator.js`) and scene planner partitioning logic (`scenePlanner.js`) to align with Google Veo's native clip limit.
  - Shifted storyboard poster generation and character reference sheet generation in `storyboardFrames.js` to Atlas Cloud when `ATLASCLOUD_API_KEY` is present.
- **Frontend changes**:
  - Registered model constants in `constants.js` and model selector card in `VideoStudio.jsx`.
  - Added option dropdown selection items and custom cost estimation calculations ($0.05/sec, Atlas Cloud tags) to Q-Ads V2 (`QAdsV2.jsx`), UGC Pro (`UGCPro.jsx`), Video Agent (`VideoAgent.jsx`), and Storyboard (`Storyboard.jsx`).
  - Integrated model into Canvas Copilot params schema (`nodeParamSchemas.js`) and `<option>` markup selector (`VideoGenerateNode.jsx`).

