# Daily Log — 2026-07-07

## Accomplishments
- Reverted all code changes related to ByteDance Seed Audio 1.0 integration in Video Studio per the user's request.
- Restored backend files to their pre-feature state:
  - `backend/agents/videoStudio/atlasClient.js`
  - `backend/routes/video-studio.js`
- Restored frontend components to their pre-feature state:
  - `frontend/src/components/VideoStudio/AdvancedMode.jsx`
  - `frontend/src/components/VideoStudio/QAds.jsx`
  - `frontend/src/components/VideoStudio/QAdsV2.jsx`
  - `frontend/src/components/VideoStudio/UGCPro.jsx`
- Fixed Q-Ads V2 control panel layout and dropdown clipping:
  - Resolved `flex-shrink` layout issues on Product, Avatar, Generate, and CfgMenu elements.
  - Enabled flex wrapping on Row 2 to handle narrow viewports cleanly.
  - Removed container horizontal overflow to prevent vertical clipping of dropdown menus.
- Configured Google DNS in backend `index.js` to ensure reliable MongoDB Atlas connection inside the local sandbox.
- Verified test users and verified build stability (`npm run build`).
- Fixed Atlas API HTTP 400 error by conditionally omitting the `resolution` parameter from Seedance reference-to-video (r2v) task payloads.
- Swapped public fallback hosting order in `backend/utils/s3.js` to try `tmpfile.link` first, avoiding Catbox and tmpfiles.org's cloud IP blocks/redirects (which caused ByteDance/Atlas to return 403 / unsupported image format errors).
- Fixed GPT Image 2 "server busy" error by setting `OPENAI_USE_LZ=false` in `.env` to route image generation requests through Atlas Cloud API instead of the exhausted/broken LaoZhang gateway.
- Fixed S3 signing bug in `getSignedUrlForPath` where public fallback URLs (like Catbox or tmpfile.link) were incorrectly wrapped in an invalid S3 presigned URL. This caused backend asset-prep downloads to fail with S3 XML/HTML errors, which were then uploaded to Atlas and rejected as unsupported formats. Public non-S3 URLs are now bypassed and returned as-is.
- Added a settings verification panel to the Storyboard review page (`Storyboard.jsx`) that displays and allows configuration of the Model, Resolution, Audio ASR Sync, and Brand DNA toggles directly before launching rendering. This gives users final validation and control.
- Fixed Seedance 2.0 Mini resolution issue in `atlasClient.js` where the `resolution` parameter was being omitted for all Seedance Reference-to-Video models. It is now only omitted for workflow-wrapped models (`atlascloud/workflow/*`), allowing direct models like `bytedance/seedance-2.0-mini` to correctly generate at `480p`.
- Enhanced Prompt Director (`storyboardDirector.js`) to support audio transcripts and precise timecodes:
  - Implemented a shared asynchronous `transcribeAudioFromUrl` helper in `video-studio.js` and consolidated transcription logic between `/agent/create` and `/storyboard/create`.
  - Added on-the-fly transcription to `/storyboard/create` if `briefAudioUrl` is provided, automatically extracting the exact video duration from timecode markers.
  - Enhanced the `storyboardDirector.js` system and user prompts to ingest the transcript, splitting the timeline into exact scenes/cuts mapped to dialogue segments.
  - Instructed the LLM to output highly cinematic multi-shot visual narratives using advanced camerawork (dolly zoom, rack focus, whip-pans, orbits), lens variations (anamorphic, macro, portrait), and creative lighting layouts (volumetric rays, rim-lights, soft diffuse shadows) instead of standard/repetitive shots.
  - Instructed the final video animation prompt builder to explicitly output timed prefix shot blocks (`[Xs-Ys]`) matching the cuts, guiding models like Seedance and Gemini Flash to render sequential storytelling shots.
- Fixed Video Model selection state persistence bug:
  - Appended the selected `videoModel` to the `FormData` payload in frontend `Storyboard.jsx` during storyboard generation.
  - Destructured `videoModel` and saved it to the `routing.selectedModel` field of `VideoProject` in backend `/storyboard/create` route, ensuring it persists in the database instead of defaulting back to `kling-3.0` during status polling or animation steps.
- Forced segment-by-segment (long-form) rendering pipeline for all multi-shot storyboard projects:
  - Updated `/storyboard/animate` to dynamically detect if a project contains multiple scenes/cuts or audio sync.
  - If so, it automatically routes the generation to the segment-by-segment stitching pipeline (forcing `isLongForm = true`), allowing all multi-cut storyboard shots to be rendered individually and combined with the audio instead of collapsing into a single-shot generation.
- Fixed generic brand/perfume poster bug during audio-driven storyboard generation:
  - Reordered the keys of the Storyboard Director LLM JSON output schema to place critical fields (`imagePrompt`, `colorPalette`, `environmentFingerprint`) at the very top (before the potentially large `cuts` array).
  - This ensures that even on long-form audio transcripts where the cuts list is truncated at the LLM's output token limits, the custom `imagePrompt` describing the storyboard setting is successfully generated and parsed instead of falling back to a generic product/perfume advertisement.
- Allowed up to 10 minutes (600 seconds) duration for storyboard generation:
  - Updated `MAX_DURATION` constant and range input slider rules in frontend `Storyboard.jsx` to let users select up to 10 minutes (600s), matching the backend limit and allowing long audio narrations to be processed fully.
- Fixed transcription truncation and short segment blow-up for long videos:
  - Migrated the audio brief ASR transcription logic in `/analyze-brief-media` from `inlineData` (which gets truncated on larger files/long audios due to direct base64 context limits) to the native **Google Files API** upload-and-transcribe pattern, ensuring the full 5.8-minute narration is successfully transcribed.
  - Increased Gemini audio transcription API `maxOutputTokens` from `2000` to `8000` to prevent long audio files from being cut off during Speech-to-Text.
  - Implemented `mergeTranscriptSegments` and `mergePreSeededCuts` helpers to group rapid, short 2-3 second dialogue chunks into highly narrative 8s chunks before passing them to the LLM. This prevents LLM output limits truncation on long-form audio narrations, maintains stable visual pacing, and generates the entire audio story storyboard.
- Fixed "AI models are currently busy" unhandled exception crash:
  - Discovered that the primary Claude model (`claude-sonnet-4-6`) failed during storyboard generation because the Anthropic API key ran out of credit balance (returning error 400).
  - Implemented automatic LLM fallback in `callMultimodalAgent` inside `agentUtils.js`. If the primary provider (e.g. Claude) fails for any reason (including out-of-credits/overload), the request automatically retries with the alternative provider (e.g. Gemini 2.5 Pro/Flash). This completely prevents unhandled crashes and ensures storyboard planning always succeeds.
- Fixed secondary audio transcription truncation during storyboard creation:
  - Migrated the `transcribeAudioFromUrl` helper function in `video-studio.js` to also use the native **Google Files API** upload-and-transcribe pattern instead of the old base64 `inlineData` (which was truncating the transcript at 67s when the storyboard was created).
  - Changed `totalDuration` in `/storyboard/create` to `Math.max(totalDuration, transcriptDur)` to prevent overriding the user's selected duration (such as 349s or 10 minutes) with a shorter timecode duration when a brief audio file is present.
- Added video compression fallback to support large video compilations when S3 is down:
  - Discovered that AWS S3 credentials in `.env` are invalid/expired, causing video uploads to fail.
  - The fallback anonymous hostings (Catbox, tmpfile, tmpfiles, file.io) rejected the uncompressed stitched mp4 (286MB) due to size limits (413 Payload Too Large).
  - Implemented `compressVideoBuffer` in `utils/s3.js` using `ffmpeg-static` to compress video files >25MB (CRF 30, superfast preset).
  - This successfully reduced the stitched video size from 286MB to 66MB, allowing public hosting fallback uploads to succeed and compiling long videos perfectly.
- Eliminated base64 data encoding from OCR and ASR analysis paths:
  - Replaced inline base64 data payload transmissions with direct file buffer uploads to the Google Files API in `/analyze-brief-media` for both image and audio briefs.
  - This prevents high memory usage, network lag, and HTTP body payload overflow errors, shifting the pipeline to a highly stable, pure binary stream pattern.
