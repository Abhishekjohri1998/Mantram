# Daily Note: 2026-06-29

## Work Accomplished
- Deep-dived the "AI models are currently busy — please try again" fallback error.
- Identified that this generic message masks any underlying backend exception or connection failure.
- Verified that prior to the June 28 fix, `this.PER_CALL_TIMEOUT_MS` was set to `45s`, which caused heavy storyboard director models to time out and fall back across all providers (Claude → Gemini → OpenAI → DeepSeek), eventually returning this generic error.
- Verified that the backend node server was not running on the host machine, which causes immediate connection failures that Vite maps to this error.
- Modified `backend/ai/router.js` to normalize the `'claude'` provider name to `'anthropic'` inside `getProvider()` and `getTextProvider()` to prevent default text provider/fallback mapping logic bleed.
- Ran backend unit tests to verify changes successfully.
- Discovered that the server running on port 3001 had been active since yesterday at 8:29 PM and had not been restarted. This meant it was still running in-memory code that was outdated and subject to the `projectId is not defined` ReferenceError (which caused the "AI models are currently busy" error in recent longform generations).
- Executed `backend/start.sh` to terminate the stale backend server process and launch a fresh server instance with all current fixes.
- Discovered that the storyboard poster generator (`storyboardFrames.js`) was hardcoded to route `gpt-image-2` to Atlas Cloud based on the presence of `ATLASCLOUD_API_KEY`, bypassing the `OPENAI_USE_LZ` toggle.
- Modified `backend/agents/videoStudio/storyboardFrames.js` (`generateWithGptImage2` and `generateCharacterReferenceSheet`) to respect `OPENAI_USE_LZ === 'true'` when deciding whether to route to Atlas Cloud or LaoZhang proxy.
- Set `OPENAI_USE_LZ=true` in `backend/.env` to route image generation back to LaoZhang to bypass Atlas Cloud 504 gateway timeouts.
- Restarted the backend server successfully via `backend/start.sh` and verified connection.
- Investigated the longform animation job failure log: caught `http 400: the parameter resolution specified in the request is not valid for model dreamina-seedance-2-0-fast in r2v`.
- Identified that Atlas Cloud's Seedance and Veo workflows (including `dreamina-seedance-2-0-fast` in reference-to-video / `r2v` mode) do not support the `resolution` parameter and fail with a validation 400 Bad Request if it's sent in the task input payload.
- Modified `submitAtlasCloudVideoGeneration` in `backend/agents/videoStudio/atlasClient.js` to dynamically check `model.includes('seedance') || model.includes('veo')` and omit the `resolution` parameter from `taskInput` if true.
- Ran backend unit tests and restarted the backend server to apply the changes.
- Investigated the user request to generate segment-level shots within a single 15-second video instead of separate 4-second cut clips.
- Modified `backend/agents/videoStudio/storyboardLongForm.js` to bypass parallel cut video generation and FFmpeg concat-stitching by setting `segmentCuts = []`. This ensures that each 10-15s segment is sent as a single unified generation to the AI video model, utilizing the composite prompt containing the sequential shot breakdown (`SHOT 1 [0s-3s]`, `SHOT 2 [3s-6s]`, etc.) created by the scene planner.
- Ran tests and restarted the server via `start.sh` to confirm proper connection.
- Investigated the character consistency issue when no custom avatar is uploaded by the user.
- Identified that the static character identity preamble in `storyboardLongForm.js` had a hardcoded `@image3` tag. However, since first frame and reference image arrays vary in size per segment, the storyboard poster index shifts dynamically (mapped as `@image2` for segments after the first one since they have no opening first-frame anchor).
- Fixed this mismatch by computing `charIdentityPreamble` dynamically inside the segment loop. We calculated the exact 1-based tag number for the poster and character sheet based on active reference lengths and first-frame offsets.
- Added a fallback character lock block for cases where the user does not upload an avatar, directing the video model to extract and lock the presenter's face shape and identity from the storyboard panels inside the poster grid (`@image1` or `@image2` depending on segment first-frame presence).
- Verified tests and restarted the server.
- Investigated the issue where prompts about a "Shiva Lingam" or "Shivling" inside an "ancient Indian temple" generated a Buddha statue.
- Identified that the aggressive deity sanitization patterns (RC#6) in `promptSanitizer.js` mapped "Shiva Lingam" and "Shivling" to generic "carved stone idol / stone sculpture" and mapped "temple" to "stone chamber". Since "Buddha statue inside a stone cave/chamber" is the most common image representation in global training datasets for these generic phrases, the AI model defaulted to rendering Buddha.
- Fixed the issue by modifying `DEITY_PHRASE_MAP` in `backend/agents/videoStudio/promptSanitizer.js` to map Shiva Lingam variants to a descriptive, non-triggering physical visual phrase: `"a smooth black cylindrical stone sculpture resting on a circular stone pedestal base"`.
- Removed the sanitization rules for `"temples"` and `"temple"` entirely to keep the word intact in the prompt. This allows the AI video models to render authentic ancient Indian temple architecture (pillars, carvings, arches) rather than a generic stone room.
- Verified all backend tests and restarted the server via `start.sh`.
- Injected Shiva temple architectural knowledge into `scenePlanner.js` system prompt to enforce authentic temple representations in fallback/planned scenes.
- Developed a robust, loophole-free incremental resume capability in `storyboardLongForm.js` that skips segment generation if successful URLs are already found in the database.
- Implemented a fair dynamic credit refund policy in `/storyboard/animate` route, calculating skipped segments and refunding the difference immediately on resubmission.
- Added self-healing capability in `/storyboard/status/:projectId` to detect animating runs stuck for >3 minutes (e.g. from server restarts), automatically mark them failed, and return the completed segment list in all status blocks.
- Committed all changes and successfully pushed the codebase to the `staging` branch.
- Bypassed `ensureMinCutsPerSegment` in `scenePlanner.js` when `structuredPlan` is present, preserving the original director-planned cuts and their exact durations so visual segments map perfectly to the transcoded/uploaded audio timeline.
- Removed the `generateMode === 'manual'` condition from `Storyboard.jsx` to render the segment items gallery in all modes (automatic and manual) and across review, animating, and complete phases, fixing the visibility on the frontend.
- Pushed the fixes to staging branch.
