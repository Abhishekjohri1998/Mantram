# 2026-06-22

## Overview
Implemented long-form video generation support for Gemini Omni Flash (`gemini-flash` and `gemini-omni-flash`) across Advanced Mode and Storyboard Studio, resolving the 10-second limit.

## Details
- Registered `gemini-omni-flash` in `falClient.js` config structures (`MODEL_AVAILABLE`, `COST_PER_SECOND`, `DURATION_LIMITS`, and `MODEL_CAPABILITIES`).
- Updated `submitVideoGeneration` in `falClient.js` and submit/resubmit handlers in `atlasClient.js` to route `gemini-omni-flash` to the Atlas Cloud Gemini Flash endpoint.
- Registered optimal (6s) and maximum (10s) segment durations for `gemini-omni-flash` in `scenePlanner.js`.
- Enabled the `/storyboard/animate` route to dynamically threshold long-form generation (`duration > 10` for Gemini Flash, `duration > 15` for other models).
- Refactored `advancedGenerateNode` in `nodes.js` to support sequential multi-segment generation and stitching using `stitchVideoClips` and last-frame extraction when `duration > 10` for Gemini models.
- Verified all files compile cleanly with syntax checks (`SYNTAX OK`).
- Pushed changes to `staging` branch successfully.

## Afternoon Session Updates
- Enabled multiple reference image upload capabilities (`multiRefImages: 6` / `refImages: true`) for `gemini-flash` model in Advanced Mode, Creative Studio config constants, and CreativeStudio page.
- Added `ref-images` feature badge representing reference image support for Gemini Flash in main Video Studio selector cards.
- Deep-dived and resolved Model and Duration dropdown select issues in Q-Ads V2:
  - Made value selection matching loose (`==`) to support string/number mismatches in duration values (e.g. `'5'` vs `5`).
  - Supported `gemini-omni-flash` and `gemini-flash` interchangeable name matches in dropdown rendering to prevent defaulting back to Seedance on load.
  - Removed React 19 `startTransition` wrapper from option click callbacks to guarantee instant synchronous state updates and prevent lag/race conditions.
  - Added CSS selection highlighting in the dropdown menu lists.

## Evening Session Updates
- Implemented template preview regeneration in Super Admin Templates Manager directly from the **Edit Template** modal:
  - Backend: Modified PUT `/:id` route in `superadmin-templates.js` to allow updating `savedPrompt` and other generation settings. Updated POST `/generate` route to check for `templateId` to update existing templates in-place rather than creating new drafts.
  - Frontend: Declared state variables and initialized them in `openEditModal` to make prompt formula, model, duration, format, and reference assets editable. Added live preview media player/thumbnail in the Edit Modal and a **Regenerate Media** button next to **Save Template**. Enabled background polling to dynamically update the preview URL upon completion.
  - Verification: Auto-merged successfully with latest main branch updates, resolved all imports, syntax-checked OK, and successfully pushed the code to staging branch.
