# Daily Memory — 2026-06-11

## Summary of Accomplishments
- **Context-Aware Image Classification**: Passed scraped product data (title, category, shortDescription) into the Stage-1 image classifier `classifyProductImageView` within `productDesignAgent.js`. This resolves a major vision LLM hallucination where folding metal device stands (like the FlexiPod) were misidentified as headphones in a vacuum. With context, it correctly identifies them as stands, which propagates correct metadata to downstream agents.
- **Robust Custom Shopify Scraper**: Added new CSS class selectors (e.g. `.productView-thumbnail-link img`, `.productView-thumbnail img`, etc.) to the `shopifyGallerySelectors` array in `brand-studio.js` to ensure the scraper extracts true product images on custom templates (like the Halo theme on `acwo.com`) and filters out policy/trust badges.
- **Verification**: Ran the entire pipeline test locally. Scraper successfully extracted the real product images (not trust icons), S3 successfully mirrored them, the classifier correctly cataloged the product as an `Adjustable Device Stand` (with 95% confidence), and `generateMoodBoardImages` used pre-signed S3 reference links to render a premium workspace mood board containing the actual device stand instead of headphones.
- **A+ / A++ Timeout Fix**: Increased the client-side `apiFetch` timeout window for `/brand-studio/aplus/generate` to 5 minutes (`300000` ms) and `/brand-studio/aplus/analyze-product` to 2 minutes (`120000` ms). The standard 30-second default timeout was aborting request fetches mid-process, even though the Node/Express backend and Vite proxy were configured to support much longer request times (up to 10 minutes) for heavy agentic pipelines.
- **A++ / Premium Tier Mismatch Fix**: Resolved the mismatch where generating an A++ listing was failing (it fell back to standard 5-module A+ listings). The frontend sent the tier as `tier: 'A+'`, while the backend route strictly expected `listingTier: 'premium'`. Fixed this by passing both parameters from the frontend (`listingTier` and `tier`) and adding dynamic tier mapping (`tier === 'A+'` -> `'premium'`) inside the backend route to ensure compatibility.
- **A+ Modules & HTML Resolution**: Discovered that the frontend `AplusTool.jsx` was attempting to map `result.modules` and render `result.html`, but the backend was returning them as `result.aplusPlan` (a raw strategy object) and had no compiled HTML field. Fixed this by implementing `buildAplusHTML` and mapped `result.modules` in the backend generator, so the response maps image URLs and contains HTML out of the box.
- **Social Kit Image Generation Fix**: Fixed a `ReferenceError: laozhangGptImageWithRefs is not defined` crash in the `/social-kit/generate` route. Added the missing `laozhangGptImageWithRefs` function to both static and dynamic imports of `laozhangClient.js` inside `brand-studio.js` so that multimodal image generation requests using GPT Image 2 execute without throwing ReferenceErrors.
- **Studio-Wide Timeout Standardization**: Standardized the `timeout` parameter on all Pulse Studio tools (Social Kit, Email, Brochure, Landing Page, Quick Post, Deck) to ensure they override the default 30-second client-side fetch timeout and prevent preemptive request aborts during long-running copy/image generation tasks.
- **Graphic Text Rendering Resolution**: Fixed the placeholder text issue where generated images in the Social Kit contained mock "Lorem Ipsum" text. Injected a Claude-generated 3-5 word headline overlay (`imageText`) into the prompt and explicitly directed the image model to render this exact text while forbidding any Latin or placeholder copy.
- **Product Deck & Website Content Synthesis**: Synthesized a comprehensive guide mapping out all 10 Mantram AI studios (and sub-studios), core technical USPs (Brand DNA, Dynamic Model Routing), competitive advantages (PDI, search grounding), and flagship features (MCP, MCoT, Fidato Nexus) into a single copy-ready artifact (`mantram_product_deck_intelligence.md`) to assist the user with deck and website content creation.
- **Website Copy & AEO/GEO Content Relaunch**: Implemented the new June 2026 copy-ready strategy across the website frontend:
  - Updated title, meta description, and `ai.summary` in `index.html` and `Landing.jsx` to reflect the 14-studio agentic OS positioning.
  - Replaced landing page FAQs (`LANDING_FAQS` in `data/studios.js` and `FAQPage` schema in `index.html`) with the 6 new structured questions.
  - Overwrote `public/llms.txt` with the concise, model-agnostic Part 7.1 graph layout for AI scrapers.
  - Consolidated `Organization` and `SoftwareApplication` JSON-LD graphs in `index.html`.
  - Updated model rosters in `IntelligenceLayer.jsx` and updated comparative pricing structures in `Comparison.jsx`.
  - Bumped sitemap dates to June 11, 2026 and finalized rendering checks.








