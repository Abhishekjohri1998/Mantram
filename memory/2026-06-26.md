# Memory Log - June 26, 2026

- **Action**: Added support for the new ByteDance `seedance-2.0-mini` (Mini) model across all video studios.
- **Backend changes**:
  - Registered `seedance-2.0-mini` capability schemas, credits structures (0.08 credits/sec flat rate), and constraints inside `falClient.js`.
  - Added resolution multiplier handling for `seedance-2.0-mini` inside `atlasClient.js`'s `resolveModelName` function.
  - Added target models list registration inside `video-studio.js` and `creatives.js`.
- **Frontend changes**:
  - Added option dropdown selection options across advanced settings, Canvas parameters, UGC cards, Q-Ads selections, and template schemas.

- **Action**: Resolved Seedance 2.0 Mini model coercion where selecting "Seedance 2.0 Mini" (referred to as Lite) defaulted to "Seedance 2.0 Fast" during background storyboard or campaign runs.
- **Backend changes**:
  - `backend/agents/videoStudio/storyboardLongForm.js`: Updated background execution to pass `model: params.model` explicitly during generation and retry phases.
  - `backend/agents/videoStudio/longFormGenerator.js`: Passed `model: params.model` to `submitAtlasCloudVideoGeneration` in all segment generation and retry blocks.
  - `backend/agents/videoStudio/graphExecutor.js`: Explicitly passed `model` to `submitAtlasCloudVideoGeneration` in `VideoGenerateHandler.execute`.
  - `backend/routes/video-studio.js`: Updated the Q-Ads safe-mode retry block to pass the correct project `model` instead of omitting it.
